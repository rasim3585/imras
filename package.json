{
  "name": "pickplay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/check-i18n.cjs && tsc -b && vite build",
    "check:i18n": "node scripts/check-i18n.cjs",
    "lint": "oxlint",
    "preview": "vite preview",
    "typecheck:cron": "tsc -p tsconfig.cron.json",
    "start": "tsx src/cron/runner.ts",
    "cron": "tsx --env-file=.env src/cron/runner.ts",
    "cron:check": "tsx --env-file=.env src/cron/check.ts",
    "cron:sync": "tsx --env-file=.env src/cron/once.ts sync",
    "cron:lambda": "tsx --env-file=.env src/cron/once.ts lambda",
    "cron:live": "tsx --env-file=.env src/cron/once.ts live"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.58.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router-dom": "^7.9.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "oxlint": "^1.71.0",
    "tsx": "^4.19.2",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
